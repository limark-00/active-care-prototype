# 主动伴护 · 网页端

认知症主动安全伴护系统的工业设计演示原型。当前实现本机 YOLO 姿态识别、区域停留、环境模拟、网页警报、事件处理闭环，以及V3.1 MacBERT文字决策实验；不用于真实照护或消防安全决策。

摄像头和 YOLO 的完整启动、试用步骤见项目根目录 `../README.md`；需要同时运行 `../vision` 本地服务。下文保留环境模拟的操作说明。

## 运行

需要 Node.js 24 和 pnpm。前端使用 React + TypeScript，开发服务基于 vinext/Vite。

```sh
cd web
pnpm install
pnpm dev
```

打开终端显示的本地地址，默认 http://localhost:3000。数据只保存在浏览器内存中，刷新即重置。右侧决策记录可导出 JSON，不包含视频或后端授权令牌；请勿在人工说明中填写真实患者信息。

```sh
pnpm test       # 纯逻辑测试，Node 原生 TypeScript 支持
pnpm typecheck
pnpm lint
pnpm build
```

## 已实现

- 摄像头、环境模拟和人工文字使用统一事件上下文。摄像头与环境事件由单请求队列自动调用部分微调MacBERT + Event Transformer；事件卡显示规则基线、AI建议、警示、置信度与融合结果。
- 非L0/I0的自由文字结果自动以“人工文字”来源进入事件中心；模型不可用、拒绝判断或返回过晚时继续按可审计规则运行。
- 温湿度、烟雾、燃气、门磁的模拟输入；每秒采样，暂停后自动检测过期。
- 同类型的持续异常合并成一个事件，烟雾/燃气报警直接进入 L4 / I4。
- 温湿度持续异常先观察，再按患者能力预设进入 I1 或 I2；网页呈现后分别使用 20 秒或 15 秒回应窗口，未回应时增加支持，不提高风险等级。
- 有限事件状态：CONFIRMING → INTERVENING → WAITING_RESPONSE / ESCALATED → RECOVERING → CLOSED。失去观测会保留事件；恢复过程中再次异常会返回处理状态。
- 网页动作的 requested / executed / failed 回执；计时从可见网页呈现开始，不把未显示当作患者未回应。
- 疑似跌倒独立 L3 / I4 事件与人工核查，系统模块状态、派生总览状态和观测质量。
- I3 模拟保护建议、手机接口 unavailable 回执；不会执行外部动作。
- 独立的患者反馈闭环：已行动、已回应、风险仍在、拒绝、主动求助；反馈绑定当轮干预，后续证据验证效果并保存多轮轨迹。
- 反馈会更新上下文版本并再次调用 V3.1；旧上下文结果只存档，确定性状态机与规则安全下限继续生效。
- 人工接手、误报原因、稳定恢复后结束事件；接手和患者回应不会混为同一状态。
- 事件日志、并发事件隔离、输入故障/离线提示、JSON 会话导出。

## 三分钟演示

1. 点击“演示温度异常”：创建 L1 / I0；持续 5 秒后变为 L2 / I1。
2. 提示在可见页面呈现后查看 20 秒倒计时；不填反馈会进入 I2，新的提示呈现后再等待会进入 I4，风险仍是 L2。
3. 新建一次温度事件并记录“已按提醒行动”。保持温度异常 10 秒，系统复核为风险仍在并增加支持；如果恢复 24°C，则进入恢复观察并把该反馈标为已验证。
4. 点击“确认接手”：停止自动未回应升级，但不会消除异常。拒绝提醒也不会触发限制。
5. 选择“需要分步图示”，再次触发温度异常：同样 L2，这次从 I2 开始，回应窗口为 15 秒。
6. 点击“演示烟雾报警”：立即 L4 / I4。暂停采样超过 3.5 秒，原报警继续保留，增加输入异常事件。恢复采样并恢复输入后，分别结束事件。

所有时间和阈值是演示配置，不是医学、消防或舒适度标准。温度触发区间 18–28°C，恢复区间 19–27°C；湿度分别为 30–70% 和 35–65%。恢复区间更窄，避免边界反复开关。

## 代码导航

| 文件                                      | 用途                             |
| ----------------------------------------- | -------------------------------- |
| `app/page.tsx`                            | 页面入口                         |
| `components/care-dashboard.tsx`           | 页面、模拟输入与操作             |
| `components/feedback-loop-panel.tsx`      | 回应倒计时、反馈按钮与多轮轨迹   |
| `lib/care/types.ts`                       | UI 无关的数据契约                |
| `lib/care/engine.ts`                      | 纯函数事件状态机、风险与干预逻辑 |
| `lib/care/camera.ts`                      | 姿态与区域证据适配，不传入图像   |
| `lib/care/overview.ts`                    | 派生总览状态与观测质量           |
| `lib/care/outputs.ts`                     | 干预内容、回执与扩展接口边界     |
| `lib/care/ai.ts`                          | 统一事件文字、请求键与模型适配   |
| `lib/care/session.ts`                     | 格式 v5 白名单会话报告           |
| `components/intervention-output.tsx`      | 网页实际呈现回执与执行历史       |
| `components/framework-panel.tsx`          | 模块状态与状态转移               |
| `components/text-decision-panel.tsx`      | V3.1文字输入、模型输出与概率展示 |
| `components/use-ai-event-orchestrator.ts` | 自动事件串行推理队列             |
| `lib/decision.ts`                         | 文字决策接口与返回值校验         |
| `tests/engine.test.ts`                    | 独立于页面的时序和安全边界测试   |
| `app/globals.css`                         | 页面主题和响应式布局             |

决策使用显式时间戳，便于测试重放。浏览器以单调计时推进时间；跳过采样的长间隔会先按数据过期处理，不能凭后台等待时间假定持续观测。

## 视觉与框架边界

- 摄像头、YOLO11n-pose 人员框和关键点已接入；基础动作由连续帧规则判断。真实模型推理与环境模拟完全分开。
- 视频只发往 `127.0.0.1:8001`。需要你主动允许摄像头；不记录视频，不做身份识别。所有结果可能误报 / 漏报。
- 人工选人、矩形地面区域、进入 / 离开、停留与疑似跌倒已接入统一事件中心；来源标注为摄像头。疑似跌倒覆盖画面中全部可识别人物，后续姿态正常也必须人工核查才能结束。
- I1/I2 仍仅网页提示，I3 不操作真实设备，I4 不联系外部人员。环境卡片仍全是模拟数据。
- 文字决策输入只发送到本机`127.0.0.1:8001`且不写磁盘。V3.1训练场景为厨房、卫生间、卧室、客厅、入户门区域和阳台；其他地点可以输入，但属于分布外测试。
- 模型由合成数据训练，结果和置信度都不是现实安全保证。AI只允许在规则安全下限之上融合；拒绝判断、低置信度或服务不可用不会删除、降低或关闭事件。
- JSON 导出包含环境 / 视觉 / 人工事件摘要、AI建议及历史、干预回执、反馈轨迹、派生状态转移、区域配置和进入 / 离开记录；不包含人工输入原文、画面或令牌。
- SAFE 仅表示当前有效范围未见目标异常。未配置区域、画面丢失或无有效对象时不能给出 L0。状态与临床安全无等价关系。
- 语音播放、米制距离、真实设备、手机通知、视频回放与跨刷新保存尚未实现。
- 摄像头延迟已进行首轮优化：默认实时预览独立刷新；识别目标上限 15 FPS，忙时丢弃帧，不积压。模型、分辨率与动作阈值不变。实际帧率 / 编码 / 往返 / 模型耗时在页面可见。
- “识别画面”保留同帧人员框与骨架；实时模式不将旧骨架或脚踝点叠到新画面上。左右翻转同步图像、标注、区域与划区输入，原始判断坐标不变。

参见根目录 `README.md` 中的“如何试用”和“验证”。

## 方向与性能的现场检查

1. 开启摄像头，分别切换“实时预览”和“识别画面”；前者不受识别刷新速度限制，后者显示同帧 AI 标注。
2. 用安全的缓慢举手动作比较左右方向；按需要切换“左右翻转”，人物框与骨架应同步，文字保持正常。
3. 划定地面区域，再切换方向；区域仍应覆盖同一实际位置。数字输入里的左右百分比始终对应当前显示方向。
4. 检查帧率与“采集至结果 / 结果年龄”，并记录自己的机位、浏览器和读数；不可将 HTTP 示例图测试的 14 FPS 当作实际摄像头保证值。
5. 停止摄像头或切换到后台，应停止采集并保留未处理事件。不要真实摔倒来做测试。
